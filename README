sanitize file and folder names by stripping unwanted characters
won't touch any files/folders starting with a dot (.)
if a file already exists it will keep appending _ to the name

sanitizew.c is for windows
sanitizeu.c is for unix
